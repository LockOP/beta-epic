# beta-epic

Fresh monorepo scaffold for the new Gen UI rebuild.

## Workspace

- `apps/web`: empty Next.js app
- `packages/ui`: empty TypeScript library

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm typecheck
```
